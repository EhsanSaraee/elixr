import styled from 'styled-components';

const Hero = () => {
   return (
      <HeroSection>
         <HeroWrapper>
            <h1>Hero</h1>
         </HeroWrapper>
      </HeroSection>
   );
};

const HeroSection = styled.section``;

const HeroWrapper = styled.div``;

export default Hero;
